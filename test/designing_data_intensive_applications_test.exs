defmodule DesigningDataIntensiveApplicationsTest do
  use ExUnit.Case
  doctest DesigningDataIntensiveApplications

  test "greets the world" do
    assert DesigningDataIntensiveApplications.hello() == :world
  end
end
